# 仓库 Agent 规则

## 代码评审契约

在宣布一个 OpenSpec change 完成或允许归档前，必须遵守以下规则：

1. 以规格为依据：读取 `openspec/changes/<change-name>/` 中的需求、场景、设计和任务清单，判断实现是否符合约定，不能只依赖聊天记忆或实现 Agent 的总结。
2. 固定审查范围：记录基线和待审提交，审查共同祖先（merge-base）到该提交的改动。可以读取其他代码理解上下文，但问题应与本次改动有关。该范围默认不包含未提交修改；存在未纳入的修改时必须明确说明，不能宣称已覆盖全部实现。
3. 运行真实验证：执行仓库规定且适用于本次改动的测试、类型检查、Lint、构建和安全扫描。分别报告通过、失败、未执行及缺少检查能力；没有执行不能算通过。
4. 独立审查：使用未参与实现的独立 Reviewer 上下文，提供规格、改动范围和必要的项目背景。实现 Agent 不得成为唯一批准者。独立上下文不要求更换模型，也不保证没有共同盲点。
5. 问题分级：按 Critical（严重阻断）、Important（重要问题）、Minor（非阻断改进）报告。这是本工作流的分级，不与 OpenSpec 的 CRITICAL、WARNING、SUGGESTION 自动一一对应，应按实际影响判断。
6. 提供具体证据：每项问题必须包含文件和行号（适用时）、触发条件、错误行为或影响，以及相关需求或规则。如果没有对应的规格条款，应说明具体的正确性或安全性依据，不得编造需求；无法定位的缺失实现应说明搜索范围和缺失证据。
7. 审查与修改分离：Review 回合只输出问题和建议，不修改代码。先形成稳定的问题清单，再进入修复回合。
8. 验证意见再修复：逐项依据代码、测试和规格确认问题，区分成立、误报和需要澄清。误报应记录拒绝理由；证据不足时不得盲目修复，也不得直接视为已解决。
9. 修复后重新验证：每项修复先运行相关检查，最终重新运行适用的完整检查，记录命令、结果和未覆盖项；必要时重新审查修复引入的改动。不得使用修复前的结果证明修复后的代码通过。
10. 明确收尾门槛：存在未解决的 Critical 时禁止归档；Important 默认阻断收尾，除非由用户明确接受风险并记录理由。必需验证失败或未完成时不得宣布通过或归档。只有 Minor 时可记录后续事项后归档。不得仅凭 AI 的“看起来正确”宣布完成。

本契约是 Agent 的执行规则，不是技术上的强制门禁。需要强制限制合并时，应由 CI、分支保护或受控脚本落实；OpenSpec Verify 本身不会自动阻止归档。

## 验证命令

所有命令均从仓库根目录运行。只有本轮实际执行且退出码为 0 的检查才能报告为通过。

### 必需自动化检查

- 单元与交互逻辑测试：`MPLBACKEND=Agg MPLCONFIGDIR=.mplconfig venv/bin/python -m unittest discover -s tests -v`
- Hover 性能冒烟检查：`PYTHONPATH=. MPLBACKEND=Agg MPLCONFIGDIR=.mplconfig venv/bin/python benchmarks/benchmark_hover.py`
  - 此命令验证 benchmark 能完整运行且无异常；当前没有机器无关的性能阈值，不能据此宣称性能指标达标。
- OpenSpec 结构校验：`openspec validate --changes`

### 条件式手工检查

- 当改动影响 QtAgg 后端、窗口生命周期或只能通过真实鼠标/键盘确认的交互时，运行 `venv/bin/python interactive_plot.py` 并记录人工检查的场景和结果。该命令会打开 GUI，不属于无头自动化检查；未执行时必须明确报告。

### 当前覆盖缺口

- 类型检查：`pyproject.toml` 包含 mypy 配置，但 mypy 未安装，也没有已采用的类型检查命令。
- Lint/格式检查：`pyproject.toml` 包含 Ruff 配置，但 Ruff 未安装，也没有已采用的 Lint 或格式检查命令。
- 构建：项目没有声明 `[build-system]`，也没有已采用的构建命令。
- 端到端测试：目前没有独立的 E2E 测试套件；现有自动化交互测试使用 Matplotlib Agg 后端。
- 安全扫描：目前没有配置 secret scan、SAST 或依赖漏洞扫描命令。

Reviewer 必须逐项报告上述检查的通过、失败、未执行或覆盖缺口状态。覆盖缺口不能写成通过；如果某个 OpenSpec change 将其中一项定义为必需验收条件，则补齐相应能力前不得宣布该 change 通过或允许归档。
